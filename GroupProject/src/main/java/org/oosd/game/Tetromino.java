package org.oosd.game;

/**
 * Enum (fixed list of constants) for all Tetris piece types.
 *
 * Each piece type will later have a rotation pattern.
 */
public enum Tetromino {
    I, O, T, S, Z, J, L
}